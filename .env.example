# Copy to .env and fill values (do NOT commit .env)
MONGODB_URI=mongodb+srv://<user>:<pass>@liquidlevel.z2sod5d.mongodb.net/?retryWrites=true&w=majority&appName=liquidlevel
MONGODB_DB=liquidlevel
CORS_ORIGIN=*
## Note: On Render, PORT is provided automatically and must not be set manually

# Optional: MQTT broker override
# If provided, this single broker will be used for all projects.
# If omitted, each project's own broker/credentials will be used (multi-broker).
MQTT_URL=tcp://broker.example.com:1883
MQTT_USERNAME=
MQTT_PASSWORD=

# Optional: FCM service account (recommended on Render)
# Paste the FULL JSON of your Firebase service account here as one line
FIREBASE_SERVICE_ACCOUNT_JSON=
# Or provide a file path to the JSON (use only one of the two)
GOOGLE_APPLICATION_CREDENTIALS=

# Optional: readings retention (days) via TTL
READINGS_TTL_DAYS=7

# Optional: bridge refresh interval in ms
BRIDGE_REFRESH_MS=60000
